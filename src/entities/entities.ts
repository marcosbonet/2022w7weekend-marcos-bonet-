export type ProtoUser = {
    name?: string;
    email?: string;
    password?: string;
    isPremium?: string;
};

export type UserTypes = {
    id: string;
    name: string;
    email: string;
    password: string;
    roll: string;
};
