export type RobotProto = {
    robotName?: string;
    velocity?: number;
    resistent?: number;
    creationDate?: string;
    img?: string;
};

export type RobotTypes = {
    id: string;
    robotName: string;
    velocity: number;
    resistent: number;
    creationDate: string;
    img: string;
};
