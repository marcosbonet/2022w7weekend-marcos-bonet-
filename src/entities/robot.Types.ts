export type RobotProto = {
    robotName?: string;
    velocity?: number;
    resistent?: number;
    creationDate?: string;
    img: string;
};

export type RobotTypes = {
    robotName: string;
    velocity: number;
    resistent: number;
    creationDate: string;
    id: number;
    img: string;
};
